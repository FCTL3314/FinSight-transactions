name: Build and Deploy

on:
  push:
    branches:
      - master

jobs:
  run-ci-checks:
    uses: ./.github/workflows/ci.yml

  build-and-push:
    needs: run-ci-checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/prod/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/finsight-transactions:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ${{ secrets.COMPOSE_FILE_DIR }}
            git pull origin master
            docker compose --env-file .env pull app
            docker compose --env-file .env up -d --force-recreate app

            echo "Waiting for the application to become healthy..."
            for i in {1..12}; do
              if [ "$(docker compose ps app | grep 'healthy')" ]; then
                echo "Application is healthy!"
                exit 0
              fi
              sleep 5
            done
            
            echo "Application did not become healthy in time."
            docker compose logs app
            exit 1